package mk.ukim.finki.akreditacii.model;

public enum SubjectDependencyType {
    PREREQUISITE,  // Subject(s) that must be passed before enrolling
    RECOMMENDED,   // Subjects recommended but not required
}